export const config = {
    /**
     * Change this to `"true"` or `"false"` depending on if you want to set the
     * attributes of the MP3 file.
     *
     * If `"true"`, FFMPEG must be on your system path.
     */
    USE_FFMPEG: true,
};
